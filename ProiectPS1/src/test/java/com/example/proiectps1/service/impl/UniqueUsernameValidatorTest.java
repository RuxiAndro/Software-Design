/*package com.example.proiectps1.service.impl;

import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.example.proiectps1.repository.UserRepository;
import com.example.proiectps1.validators.UniqueUsernameValidator;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class UniqueUsernameValidatorTest {

    @Autowired
    private UniqueUsernameValidator uniqueUsernameValidator;

    @Autowired
    private UserRepository userRepository;

    @Test
    public void testUniqueUsernameValidatorInjection() {
        // Verifică dacă UserRepository este injectat corect în UniqueUsernameValidator
        assertNotNull(uniqueUsernameValidator);
        assertNotNull(userRepository);

        // Poți adăuga teste suplimentare pentru validarea funcționării metodei isValid
    }
}
*/